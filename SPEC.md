# タップ&スマッシュ SPEC.md

このドキュメントは「タップ&スマッシュ」アプリの仕様書です。
非エンジニアの発注者とのヒアリング内容をもとに作成しました。
実装フェーズでは、このSPEC.mdを唯一の正として作業してください。

---

## 1. アプリ概要

スマホのブラウザで開くと、すぐに「タップ・スマッシュ」で発散できるミニゲームが始まる、
ストレス発散専用のシンプルなWebアプリ。

**解決したい問題**
怒りや衝動的な感情を、家族・同僚・友人など身近な人にぶつけてしまう前に、
安全かつ即座に発散できる手段を提供する。「対人トラブルの予防弁」としての位置づけ。

**コンセプト**
- 説明を読まなくても、開いた瞬間に直感的に使える
- 我慢の限界が近いときに、数十秒〜数分ですぐ使える
- 発散すること自体が目的。記録・分析・他者との比較は目的にしない

---

## 2. ターゲット

**メインターゲット**：仕事・家事・育児などで日常的にストレスを抱える大人

- 年齢層は幅広い成人（学生や特定の年代に限定しない）
- ITリテラシーは高くない前提。説明書を読まずに使えることが必須
- 「今すぐ、この場で」発散したい人（自宅・職場・移動中など）

---

## 3. ユーザーが何をするか（ユーザーストーリー）

1. イライラや衝動を感じたとき、スマホでアプリのURL（またはホーム画面のアイコン）を開く
2. 開くとすぐにミニゲーム画面が表示され、待たされることなく発散を始められる
3. 画面をタップ・スワイプして、風船割りや紙くしゃくしゃなどの動作でストレスを発散する
4. 気分に応じて、2〜3種類の発散方法を切り替えて試せる
5. 気が済んだら、そのままアプリを閉じる（ログアウトや保存操作は不要）

---

## 4. 全機能

### 4-1. 今回作る機能（V1スコープ）

| 機能 | 内容 |
|---|---|
| ミニゲーム：風船割り | 画面をタップすると風船が割れる。連続タップで複数の風船を割れる |
| ミニゲーム：紙くしゃくしゃ | 画面上の紙を指でこすったり握る動作（ドラッグ/タップ）でくしゃくしゃに丸められる |
| ミニゲーム：3つ目の発散方法 | 実装フェーズで具体案を決定（例：太鼓/的叩きなど、指タップで完結するもの） |
| 発散方法の切り替え | 画面下部などのタブ/ボタンで、上記2〜3種類をその場で切り替えられる |
| 効果音・視覚エフェクト | タップ・スマッシュ時に、破裂音や視覚的な演出（パーティクル、揺れなど）を再生する |
| フォールバック動作 | 音やエフェクトが読み込めない場合でも、タップ操作とゲームの反応自体は必ず動作する |

### 4-2. 今回は作らない機能（将来の拡張候補としてメモのみ）

以下は「あったら嬉しいが、今回のスコープには含めない」機能です。
将来的に追加を検討する場合の候補としてSPEC上にメモしておきます。

- 発散回数・強度の記録とグラフ化（継続利用を促す機能）
- 呼吸法・瞑想などのリラクゼーションガイド機能
- SNSシェアやランキングなどのソーシャル機能

---

## 5. 画面一覧

**画面構成：最小構成（1画面のみ）**

| 画面名 | 役割 |
|---|---|
| ミニゲーム画面（メイン画面 兼 唯一の画面） | アプリを開いたら即座に表示される、発散操作を行う画面。画面内に発散方法の切り替えUI（2〜3種類）を含む |

- ホーム画面・設定画面・ランキング画面などは作らない
- 「開く→即発散できる」を最優先するため、画面遷移や選択のステップを挟まない設計とする

---

## 6. データ構造

**方針：永続化データは一切保存しない（完全にその場限り）**

- サーバーへの送信、ブラウザのローカルストレージへの保存、Cookie等への保存は行わない
- アプリを閉じる・再読み込みすると、状態は初期化される
- 保存が必要なのは以下のような「その場限りの画面内の状態」のみで、これはメモリ上の変数で管理し、永続化はしない

```
（永続化なし・画面内メモリ上のみの一時状態の例）
- currentGame: "balloon" | "paper" | "third_game"  ← 現在選択中の発散方法
- soundEnabled: true | false                        ← 音声読み込みに成功しているか
```

---

## 7. 外部連携

- **ログイン・アカウント連携：なし**（不要）
- **外部API連携：なし**（実行時に外部サーバーへ通信することはしない）
- **効果音・エフェクト素材：外部の無料素材サイト等から取得した音声ファイルを、アプリ内にあらかじめ同梱（バンドル）して使用する**
  - 実行時に外部サイトへアクセスする方式ではなく、開発時にダウンロードしてアプリ内のファイルとして組み込む
  - 理由：ログイン不要・通信不要にすることでシンプルさと信頼性（オフラインでも動く）を両立するため
  - 使用する素材は、商用利用・改変可能なライセンス（フリー素材）のものに限定する

---

## 8. 対象外（作らないもの）

以下は「今回作らない」だけでなく、「将来的にも作る予定がない」と明言されたものです。

- 他ユーザーとのチャット・フレンド機能
- AIチャット・カウンセリング機能（相談相手やアドバイスを返す機能）
- 課金・広告表示（完全無料・広告なしのシンプルなアプリとする）
- ログイン・アカウント機能
- サーバー側でのデータ保存・個人情報の収集

---

## 9. 技術構成

**実装方式：Webアプリ（ブラウザで動作）**

| 選択肢 | メリット | デメリット |
|---|---|---|
| **Webアプリ（採用）** | アプリストア審査不要。URLを開くだけ、またはホーム画面に追加するだけで使える。Claude Codeで最も早く形にできる | 一部センサー系機能（振動・シェイク検知など）はOSの制限で使いにくい場合がある |
| ネイティブアプリ | スマホの機能をフルに使える。ストアからインストールできる | 開発が複雑で時間がかかる。審査や開発者登録（有料）が必要で、非エンジニアにはハードルが高い |

**技術スタック（実装フェーズの目安）**

- HTML / CSS / JavaScript（素のWeb技術。ビルド環境や複雑なフレームワークは不要な範囲で構成する）
- レイアウトはスマホ画面を基準にしたモバイルファースト設計（PCでの表示崩れ防止程度の最低限のレスポンシブ対応は行う）
- 効果音再生には `<audio>` タグ等、標準的なブラウザ機能を使用する
- ホーム画面に追加してアイコンから起動できるようにする（PWA的な最低限の対応。ネイティブアプリ化はしない）

**利用デバイス**：スマホ中心（タップ・スワイプなど指での操作を前提に設計する。PCではマウスクリックで代用可能な範囲でよい）

---

## 10. セキュリティ要件

- 個人情報（氏名・メールアドレス・パスワードなど）は一切収集・保存・送信しない
- ログイン機能がないため、認証情報を扱う処理自体が存在しない
- 外部通信を行わない（効果音素材はアプリ内に同梱するため、実行時の外部送信は発生しない）
- 上記の前提により、本アプリのセキュリティリスクは低いと判断する

---

## 11. 完成条件

以下をすべて満たした状態を「完成」とする。

1. スマホのブラウザでアプリを開くと、即座にミニゲーム画面が表示される
2. 2〜3種類の発散方法（風船割り・紙くしゃくしゃ・3つ目の発散方法）が、画面内の切り替えUIで問題なく切り替えられる
3. 各発散方法で、タップ・スワイプなどの操作に対してゲームが正しく反応する（連続操作でも破綻しない）
4. 効果音・視覚エフェクトが一通り実装され、演出として違和感なく動作する
5. 効果音やエフェクトが万一読み込めない場合でも、タップ・スマッシュ操作自体は必ず動作する（フォールバックが機能する）
6. データを保存しない設計になっている（ログイン画面・保存確認・エラーによる個人情報のやり取りが発生しない）

---

## 12. 最初から最後まで通しで動作確認する手順（QA手順）

実装完了後、以下の手順を順番に実施し、すべて問題なく通ることを確認する。

1. **起動確認**
   - スマホのブラウザでアプリのURLを開く
   - 画面が真っ白にならず、ミニゲーム画面が数秒以内に表示されることを確認する

2. **1つ目の発散方法（風船割り）の確認**
   - 画面をタップし、風船が割れる／消えるなどの反応があることを確認する
   - 連続で素早くタップしても、動作が固まったり画面が崩れたりしないことを確認する
   - タップ時に効果音・エフェクトが再生されることを確認する

3. **2つ目の発散方法（紙くしゃくしゃ）の確認**
   - 切り替えUIから紙くしゃくしゃに切り替える
   - タップ／ドラッグ操作で紙がくしゃくしゃになる反応があることを確認する
   - 効果音・エフェクトが再生されることを確認する

4. **3つ目の発散方法の確認**
   - 切り替えUIから3つ目の発散方法に切り替える
   - 想定した操作（タップなど）に対して正しく反応することを確認する
   - 効果音・エフェクトが再生されることを確認する

5. **切り替えの往復確認**
   - 1→2→3→1のように発散方法を何度か往復して切り替え、表示や動作が崩れないことを確認する

6. **エラー時のフォールバック確認**
   - 機内モードにする、または音声ファイルを一時的に読み込めない状態にして、効果音が鳴らない状況を再現する
   - その状態でも、タップ操作とゲームの反応（見た目の変化）自体は正常に動作することを確認する

7. **データが残らないことの確認**
   - ひとしきり操作した後、アプリを再読み込み（リロード）する
   - 前回の操作内容や状態が一切残っておらず、最初の状態に戻ることを確認する
   - ブラウザの開発者ツール等で、ローカルストレージやCookieに何もデータが保存されていないことを確認する（任意・可能であれば）

8. **PCブラウザでの簡易確認**
   - PCのブラウザでも開いてみて、画面が極端に崩れていないこと、マウス操作で代用して一通り動くことを確認する（PC最適化までは不要）

上記すべてが問題なく完了した時点で「完成」とする。

---

## 13. 将来の拡張候補（今回は実装しないが、参考としてメモ）

- 発散回数・強度の記録とグラフ化
- 呼吸法・瞑想などのリラクゼーションガイド機能
- SNSシェアやランキングなどのソーシャル機能
- ログイン機能や外部サービス連携（利用状況の同期など）を将来追加する場合は、個人情報保護の観点から改めて要件を洗い出すこと
